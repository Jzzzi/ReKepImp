device: &device 'cuda'
seed: &seed 0
bounds_min: &bounds_min [-10.0, -10.0, -10.0]
bounds_max: &bounds_max [10.0, 10.0, 10.0]

keypoint_proposer:
  num_candidates_per_mask: 5
  min_dist_bt_keypoints: 0.06
  max_mask_ratio: 0.5
  device: *device
  bounds_min: *bounds_min
  bounds_max: *bounds_max
  seed: *seed

realsense:
  instrinsics: [386.74, 0, 326.66, 0, 386.28, 247.33, 0, 0, 1]
  height: 480
  width: 640
  fps: 30

